# Mac CommondX 依赖

# ==================== 核心运行时依赖（必需）====================
pyobjc-core>=12.0
pyobjc-framework-Cocoa>=12.0
pyobjc-framework-Quartz>=12.0
pyobjc-framework-ApplicationServices>=12.0
PyYAML>=6.0
cedar>=1.3.0

# ==================== 打包工具 ====================
pyinstaller>=6.0

# ==================== 可选功能依赖 ====================
# HTTP 请求库（Kimi API 调用）
requests>=2.31.0
# OpenAI SDK（Kimi/Moonshot API 调用，推荐使用）
openai>=1.0.0
# MD 转 HTML/PDF/图片功能（代码中有降级处理，未安装时使用简单转换）
markdown>=3.0
weasyprint>=60.0  # PDF 转换工具（纯 Python）
reportlab>=4.0  # PDF 生成备选工具（纯 Python）
html2image>=2.0.0  # HTML 转图片工具（轻量级，无需完整浏览器）
# PDF 转 Word 功能（纯 Python 实现）
pdf2docx>=0.5.0  # PDF 转 Word（纯 Python）
# Word 转 PDF 功能
docx2pdf>=0.1.8  # Word 转 PDF（需要系统安装 Microsoft Word 或 LibreOffice）
python-docx>=1.1.0  # Word 文档处理（用于降级方案，配合 reportlab 使用）
# 压缩/解压功能（纯 Python 实现）
py7zr>=0.21.0  # 7Z 解压（纯 Python）
rarfile>=4.0  # RAR 解压（需要系统有 unrar 工具，但通过 Python 包管理）

# ==================== 开发工具依赖 ====================
# 图标生成工具 (tools/generate_icons.py)
pillow>=10.0

# DMG 背景图生成工具 (tools/generate_dmg_background.py)
opencv-python>=4.8.0
numpy>=1.24.0

# 图像处理相关（cedar.draw 可能依赖）
matplotlib>=3.7.0